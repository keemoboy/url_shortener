<%= form_for(@shorten_url) do |f| %>
  <% if @shorten_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@shorten_url.errors.count, "error") %> prohibited this shorten_url from being saved:</h2>

      <ul>
      <% @shorten_url.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :source_url %><br />
    <%= f.text_area :source_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
